# =========================================================
# PRODUCTION environment variables
# Vite automatically loads this file when running `vite build`
# =========================================================

# Backend — your deployed Render service
# This makes the onramp / offramp webhook URLs resolve to:
#   https://defi-corre.onrender.com/api/webhooks/paj-onramp
#   https://defi-corre.onrender.com/api/webhooks/paj-offramp
VITE_BACKEND_URL=https://defi-corre.onrender.com
